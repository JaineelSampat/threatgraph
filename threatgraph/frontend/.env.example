# Leave empty for local dev (Vite proxies /api to localhost:8000, see vite.config.ts).
# In production, set this to your deployed backend's base URL, e.g.:
# VITE_API_BASE_URL=https://threatgraph-api.onrender.com
VITE_API_BASE_URL=
